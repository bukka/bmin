# TODO file

# CODE -------------------------------------------------

termtree.h, termtree.cpp
	- comleting, test

termscontainer.cpp
	TermsContainer::getTermsIdx - Term ZERO

formula.cpp
	constructors
	Formula::toString(bool idx_form) - rewrite - compatibility with parse
	Formula::checkVars - variable unique
	
term.h
	OutputValue -> standalone class
	
parser.cpp
	product of sums
	
	
# FEATURES ---------------------------------------------

more formulas
parsing all file (no every row standalone)
